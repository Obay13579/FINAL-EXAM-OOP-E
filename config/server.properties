# Server Configuration
server.port=12345
server.maxClients=50
server.timeout=30000

# Database Configuration
db.url=jdbc:mysql://localhost:3306/collabdraw
db.user=root
db.password=password
db.maxPoolSize=10
db.connectionTimeout=30000

# Logging Configuration
logging.level=INFO
logging.file=logs/server.log
logging.pattern=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Canvas Settings
canvas.maxWidth=1920
canvas.maxHeight=1080
canvas.defaultWidth=800
canvas.defaultHeight=600

# Chat Settings
chat.maxMessageLength=500
chat.historySize=100